apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ingestion-chart.fullname" . }}-test-connection"
  labels:
    {{- include "ingestion-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ingestion-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
