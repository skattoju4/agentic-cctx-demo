apiVersion: v1
kind: Service
metadata:
  name: {{ include "ingestion-service-ts.fullname" . }}
  labels:
    {{- include "ingestion-service-ts.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ingestion-service-ts.selectorLabels" . | nindent 4 }}
