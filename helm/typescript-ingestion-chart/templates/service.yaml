apiVersion: v1
kind: Service
metadata:
  name: {{ include "typescript-ingestion-chart.fullname" . }}
  labels:
    {{- include "typescript-ingestion-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "typescript-ingestion-chart.selectorLabels" . | nindent 4 }}
